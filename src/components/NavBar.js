import { Link } from 'react-router-dom';

export default function NavBar () {
    return(
        <nav>
            <a to='/'>f</a>
            <a to='/'>g</a>
            <a to='/'>g</a>
            <a to='/'>g</a>
        </nav>
    );
}